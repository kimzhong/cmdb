# CMDB 平台需求文档

基于二丫讲梵 CMDB 平台建设指南整理

---

## 一、CMDB 平台概述

### 1.1 什么是 CMDB
CMDB（Configuration Management Database）即配置管理数据库，又称资产管理系统。

### 1.2 核心目标
- **使用角度**：解决运维乃至研发在日常工作中的基础查询需求，作为运维规范、流程固化实施推行的舞台
- **核心设计**：以资源数据为中心，将云上数据和本地数据维护起来；以应用为核心，成为本地应用与云端服务器、域名、数据库等资源关联起来的桥梁

### 1.3 建设原则
- **维护简单**：减少维护成本，支持运维规范化的制定及推行
- **数据准确**：CMDB 的立身之本，是越用功能才越完善
- **灵活可靠**：通过模型体系解决灵活性问题

---

## 二、技术选型

### 2.1 数据库选型
- **MongoDB**：选型原因
  - 动态字段：模型的字段是动态灵活的，MongoDB 的 BSON 格式刚好契合
  - 全文搜索：CMDB 80% 是查询场景，MongoDB 原生支持全文模糊搜索

### 2.2 技术栈
- **后端**：Go + Gin 框架 + mongo-driver
- **前端**：React + Ant Design 框架

---

## 三、系统导航

```
CMDB
├── 全局搜索
├── 应用视图
├── 资源模型
│   ├── 模型管理
│   ├── 标签管理
│   └── 资源仓库
└── 云管中心
    └── 定时任务
```

---

## 四、第一阶段：模型管理与资源仓库

### 4.1 模型管理

#### 4.1.1 模型四大分类
- 资产模型
- 应用模型
- 组织模型
- 其他

#### 4.1.2 模型分组管理
- 新建模型分组
- 修改模型分组
- 删除模型分组（分组内有模型则不能删除）

#### 4.1.3 模型详情
- 展示模型基础信息（唯一标识、名称、是否内置）
- 编辑模型
- 停用模型
- 删除模型（模型下有数据则不能删除）
- 添加字段
- 模型创建完成后自动创建两个分组：基本属性、关系属性
- 模型创建完成后自动创建两个字段：唯一标识、名称

#### 4.1.4 模型字段分组管理
- 新建字段分组
- 编辑字段分组
- 删除字段分组（分组下有字段则不能删除）

#### 4.1.5 模型字段类型（6种）
1. **字符串**：长度 256 个英文字符或 85 个中文字符
2. **数字**：正负整数
3. **日期**：日期格式
4. **下拉选项**：包含 K-V 结构的列表
5. **密码**：数据要求后端加密保存，展示时密文透出
6. **关系**：从属关系、连接关系

#### 4.1.6 关系类型
- **从属关系**：一对一的关系，一个模型只能从属于某一个模型（如主机关联子网网段）
- **连接关系**：一对多、多对多、多对一，一个模型可以关联多个模型（如主机关联磁盘、安全组）

#### 4.1.7 字段属性
- 名称、唯一标识、字段类型
- 校验规则：正则校验（如邮箱、手机号）
- 字段设置：是否必填、是否内置
- 编辑字段：只能修改名称和描述，字段类型与标识不能修改
- 删除字段：模型内字段有数据则不能删除

### 4.2 资源仓库

#### 4.2.1 首页布局
- 分成资源模型、应用模型、组织模型、其他四大类
- 每个归类下通过模型分组进行展示（分组名、模型列表、数据总数）
- 点进去查看资源详情

#### 4.2.2 资源列表功能
- 资源以列表形式展示
- 可通过不同字段进行搜索
- 可以手动创建数据
- 数据管理：查看、编辑、删除
- 数据的批量删除
- 提供数据的添加标签能力

#### 4.2.3 资源详情
- 资源详情：与模型一致，按字段分组进行展示
- 关系管理：分成从属和连接两种关系进行展示与维护
- **重要**：关联的资源在唯一标识上添加超链接，可跳转到对应资源详情页

---

## 五、第二阶段：定时任务与全局搜索

### 5.1 定时任务

#### 5.1.1 定时任务管理
- 添加任务
- 同步策略：全量和增量两种
- 分类：云上资源、其他资源
- 云上资源配置：选择需要同步的可用区数据
- 编辑任务
- 删除任务

#### 5.1.2 同步流程
1. 配置 scanType 的映射域名
2. 通过云控制台拿到远程数据
3. 本地创建该对象的模型
4. 配置远端和本地模型字段的映射关系
5. 编码开发具体同步逻辑
6. 在定时任务管理页面添加任务

#### 5.1.3 全量与增量设计
- **全量同步**：本地与远端保持强一致
- **增量同步**：如果远端没有但本地还有的数据，通过标签系统做标识，由运维手动确认

### 5.2 全局搜索
- 单独的页面提供一个搜索框
- 输入关键词对所有数据表进行全局搜索
- 基于 MongoDB 全文索引实现

---

## 六、第三阶段：标签管理与应用视图

### 6.1 标签管理

#### 6.1.1 核心逻辑
- 标签键（TagKey）：唯一的键
- 标签值（TagValue）：不必唯一，一个标签键可关联多个标签值
- 标签值可关联多个资源数据

#### 6.1.2 标签键管理
- 创建标签键、标签说明
- 添加标签值
- 编辑标签键
- 删除标签键

#### 6.1.3 标签值管理
- 新建标签值
- 绑定资源：选择模型 -> 选择要关联的数据
- 更新标签值
- 删除标签值（标签关联有数据则不能删除）
- 关联的数据唯一标识添加超链接，可跳转到数据详情页

#### 6.1.4 标签搜索
- 资源类型：默认过滤所有模型，选择后只过滤对应模型
- 标签选择：选择要过滤的标签
- 查询结果按模型分栏展示

### 6.2 应用视图

#### 6.2.1 左侧业务与应用树
- 一级菜单：根路由
- 二级菜单：业务
- 三级菜单：应用（归属于某个业务）
- 最多三级
- 可通过业务或应用名搜索

#### 6.2.2 应用详情
- 应用关联的主机
- 应用关联的域名
- 应用关联的中间件
- 以上资源均可通过标签进行环境过滤

#### 6.2.3 环境管理（通过标签实现）
- 创建环境标识标签键（environment）
- 标签值：test（测试环境）、pre（预发环境）、prod（正式环境）
- 通过标签过滤区分不同环境的主机列表
- 建议：一个资源实例不能同时属于多个环境标签值

---

## 七、模型建设流程

### 7.1 步骤
1. **梳理资源**：将运维环境中的资源进行梳理，绘制资源对象图
2. **梳理字段**：使用表格维护模型信息、模型字段、模型关系
3. **同步对齐**：运维组内成员共同讨论、评审
4. **平台上建立模型**：在平台创建模型后维护数据

### 7.2 删除约束（保障数据准确）
- 删除模型分组：确保分组下没有模型
- 删除模型：确保模型下没有字段分组
- 删除字段分组：确保分组下没有字段
- 删除字段：确保字段没有数据

---

## 八、应用生命周期管理（扩展功能）

### 8.1 应用创建
- 通过工单系统申请
- 表单内容为应用对应字段
- Leader 审核：核对应用名是否符合规范、资源需求是否合理
- 通过后自动创建 GitLab 项目
- 应用状态：待上线 -> 运行中
- 运维进行资源准备、环境准备
- 所有配套流程完成后才能将状态改为"运行中"

### 8.2 应用下线
- 通过工单系统申请
- 运维执行 checklist 核查
- 将应用状态置为"已下线"（不删除，保持数据可查阅）

---

## 九、开发经验

### 9.1 MongoDB 自增 ID
- 使用另一张表存放 ID
- 通过 findAndModify 方法获取并加 1（具有原子性）

### 9.2 全局搜索实现
- **数组索引**：对数组字段建立索引提高检索效率
- **全文索引**：`db.collection.createIndex({"$**":"text"})`
- 支持多条件逻辑或（`/`）、逻辑与（`"word1 word2"`）、排除（`-word`）

### 9.3 平台推广
- **自身硬**：围绕运维痛点设计
- **常吹风**：项目进展同步、引导体验
- **强约束**：借助领导力量、重要功能通过平台完成

---

## 十、当前项目实现状态

### 已实现功能
- [x] 模型管理（四大分类、模型分组、模型详情、模型字段）
- [x] 资源仓库（资源列表、CRUD、关系管理）
- [x] 标签管理（标签键、标签值、资源绑定）
- [x] 应用管理（基本 CRUD）
- [x] 定时任务（任务管理）
- [x] 全局搜索

### 待实现功能
- [x] 模型字段的完整类型支持（密码加密已实现、关系类型已实现）
- [x] 资源关系管理（从属关系、连接关系的完整实现）
- [ ] 定时任务同步逻辑（云资源同步）
- [x] 应用视图（业务树、应用与环境标签关联已实现）
- [ ] LDAP/AD 集成
- [x] 资源详情的超链接跳转（关联资源点击跳转）

---

*文档整理基于：https://wiki.eryajf.net/pages/4bcf72/*
