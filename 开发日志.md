# CMDB开发日志

## 2026-06-06 测试与代码质量专项

### 背景

基于 `requirement.md`（CMDB 配置管理平台）10 章需求规格，对项目做全量 code review + simplify 清理 + 单元测试，并系统验证 8 个前端页面的 CRUD 流程。本次只增不改已有功能，按用户要求保持 router.go 单文件结构。

### 今日完成

#### 1. 全量 code-review（10 条 finding，按严重度）

| # | 文件:行 | 严重度 | 问题 |
|---|---------|--------|------|
| 1 | services/model.go:479 | CRITICAL | 死代码含 `func errors.New(...)` 非法 Go 语法 |
| 2 | Tag.vue:256 | CRITICAL | tagKeyColumns dataIndex 与 loadTagKeys 映射不一致 |
| 3 | Resource.vue:1 | CRITICAL | Search.vue 跳 query 不被消费 |
| 4 | router.go:1252 | HIGH | globalSearch 用户 keyword 未转义 → ReDoS |
| 5 | router.go:1277 | HIGH | globalSearch 串行 N+1 FindOne |
| 6 | router.go:992 | HIGH | batchDeleteResources 无 ownership 检查 |
| 7 | middleware/cors.go:13 | MEDIUM | CORS 硬设 Content-Type 污染所有响应 |
| 8 | router.go:1262 | MEDIUM | globalSearch fallback 用 fmt.Sprintf+Contains 误匹配 |
| 9 | router.go:1016 | MEDIUM | batchDeleteResources 静默吞非法 ID |
| 10 | router.go:1402 | LOW | createTagValue 时间戳补丁应通用化 |

#### 2. simplify 阶段：8 项修复

- 后端死代码清理（services/{model,user,resource}.go 编译错误）
- `cors.go` 移除 Content-Type 污染
- `router.go` globalSearch 用 `regexp.QuoteMeta` + 64 字节限长
- `router.go` batchDeleteResources 收集 invalid_ids + 全非法 400
- `Tag.vue` tagKeyColumns dataIndex 修复
- `Resource.vue` onMounted 读 query 自动打开详情

#### 3. 单元测试：48 个测试用例

**后端（19）**：
- `internal/middleware/auth_test.go` — 7 测试：JWT 生成/解析/过期/中间件
- `internal/middleware/cors_test.go` — 3 测试
- `internal/routers/helpers_test.go` — 6 测试：DES 加解密、正则、参数解析
- `internal/services/integration_test.go` — 4 测试（build tag: integration，MongoDB 不可达时 skip）

**前端（29→31）**：
- `api/__tests__/interceptors.spec.js` — 6
- `store/__tests__/store.spec.js` — 9
- `router/__tests__/guard.spec.js` — 7
- `views/__tests__/Search.spec.js` — 5
- `views/__tests__/Tag.spec.js` — 3
- `views/__tests__/Resource.spec.js` — 2（本次新增，复现用户反馈的 mount 错误）

#### 4. 集成测试驱动发现 3 个新 bug

启动本地 MongoDB 跑集成测试，发现 router.go 真实业务 bug：

| 文件 | 症状 | 修复 |
|------|------|------|
| `services/resource.go` BindResource | `$addToSet` 在 null 字段崩溃 | pipeline + `$ifNull` + 双向同步 |
| `router.go` bindAppResource | `$addToSet` 在 null 字段崩溃 | 同上 pipeline 模式 |
| `router.go` unbindAppResource | `$pull` 在 null 字段崩溃 | 加 `$type:array` 过滤 |

#### 5. E2E CRUD 全页面验证（86/86 通过）

启动 MongoDB + 后端 + 前端，写 E2E 脚本验证 8 个页面的 CRUD：

| 页面 | 用例 | 状态 |
|------|------|------|
| Login | 1 | ✓ |
| Dashboard | 1 | ✓ |
| Model（分组/模型/字段/关系） | 21 | ✓ |
| Resource（CRUD/批量/关系） | 13 | ✓ |
| Tag（键/值/绑定/搜索） | 13 | ✓ |
| Search（全局/安全） | 5 | ✓ |
| App（业务/应用） | 13 | ✓ |
| Task（同步任务） | 7 | ✓ |
| 边界与安全 | 3 | ✓ |
| 清理 | 3 | ✓ |
| **总计** | **86** | **✓ 全过** |

#### 6. 用户反馈修复（页面报错）

- `web/vue.config.js` 加 `historyApiFallback: true` — 解决直接访问 /resource 等深层路径 404
- `web/src/views/Resource.vue` 修复 `useRoute()` 在 `onMounted` 中调用导致的 "Cannot read properties of undefined (reading 'query')" 错误

### 启动命令

```bash
# MongoDB
"C:\Program Files\MongoDB\Server\8.2\bin\mongod.exe" --dbpath D:\mongodb\data --logpath D:\mongodb\log\mongod.log --port 27017 --bind_ip 127.0.0.1

# 后端
cd d:\minimax\cmdb && go run cmd/server/main.go

# 前端
cd d:\minimax\cmdb\web && npm run serve
# 浏览器打开 http://localhost:3000
```

### 测试命令

```bash
# 后端单元
go test ./...

# 后端集成（需 MongoDB）
go test -tags=integration ./...

# 前端
cd web && npm test

# E2E CRUD（需 MongoDB+后端+前端）
bash /c/Users/kim/AppData/Local/Temp/cmdb_e2e_test.sh
```

---

## 2026-03-14 开发记录

### 今日完成

1. **前端与后端联调**
   - 创建vue.config.js配置代理，解决前后端跨域问题
   - 添加Dashboard统计数据API (/api/v1/stats)
   - 更新Dashboard页面，动态获取统计数据

2. **应用视图功能**
   - 后端添加应用管理API (CRUD + 资源关联)
     - GET/POST /api/v1/apps - 应用列表/创建
     - GET/PUT/DELETE /api/v1/apps/:id - 应用详情/更新/删除
     - GET /api/v1/apps/:id/resources - 应用关联资源
   - 前端创建App.vue应用管理页面
   - 添加前端API和路由

3. **定时任务功能**
   - 后端添加定时任务API (CRUD + 执行)
     - GET/POST /api/v1/tasks - 任务列表/创建
     - GET/PUT/DELETE /api/v1/tasks/:id - 任务详情/更新/删除
     - POST /api/v1/tasks/:id/run - 手动执行任务
   - 前端创建Task.vue任务管理页面
   - 支持阿里云、腾讯云、华为云、AWS等云类型
   - 支持全量/增量同步模式
   - 支持Cron表达式定时执行

4. **编译验证**
   - 后端编译成功
   - 前端构建成功

### 待完善

1. LDAP/AD实际配置测试 (需要实际的LDAP/AD服务器)
2. 应用与资源的关联关系管理
3. 定时任务的实际同步逻辑实现

### 启动命令

```bash
# 后端
cd D:\minimax\cmdb
go run cmd/server/main.go

# 前端
cd D:\minimax\cmdb\web
npm install
npm run serve
```

---

## 2024-03-13 开发记录

### 项目概述

基于 Golang + Vue + MongoDB 开发 CMDB 配置管理平台，参考eryajf的CMDB建设指南。

### 技术栈

- 后端: Go + Gin框架 + MongoDB
- 前端: Vue 3 + Ant Design Vue
- 认证: JWT + LDAP/AD

### 项目结构

```
cmdb/
├── cmd/server/main.go        # 后端入口
├── config/
│   ├── config.go            # 配置加载
│   └── mongodb.go           # MongoDB连接
├── database/
│   └── mongodb.go           # 数据库操作
├── internal/
│   ├── models/models.go     # 数据模型
│   ├── middleware/
│   │   ├── auth.go         # JWT认证
│   │   └── cors.go         # 跨域中间件
│   ├── routers/router.go   # API路由
│   └── services/
│       ├── ldap.go         # LDAP/AD服务
│       ├── user.go         # 用户服务
│       ├── model.go        # 模型服务
│       └── resource.go     # 资源服务
├── config.yaml              # 配置文件
├── go.mod                  # Go模块
└── web/                   # Vue前端
```

### 今日完成

1. **后端开发**
   - 用户认证系统（本地+LDAP/AD）
   - 模型管理（模型分组、模型、字段管理）
   - 资源仓库（CRUD、关系管理）
   - 标签管理（标签键/值、资源绑定）
   - 全局搜索

2. **前端开发**
   - 登录页面
   - 仪表盘
   - 模型管理页面
   - 资源仓库页面
   - 标签管理页面
   - 全局搜索页面

3. **编译运行**
   - 修复编译错误:
     - 修复models变量命名与models包的冲突
     - 修复未使用的导入
     - 修复LDAP认证错误返回值
   - 成功编译并运行

4. **测试验证**
   - 用户注册: POST /api/v1/users/register ✓
   - 用户登录: POST /api/v1/login ✓

### 待完善

1. 前端启动并与后端联调
2. 应用视图功能
3. 定时任务功能
4. LDAP/AD实际配置测试

### 启动命令

```bash
# 后端
cd D:\minimax\cmdb
go run cmd/server/main.go

# 前端
cd D:\minimax\cmdb\web
npm install
npm run serve
```

### API接口列表

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/login | 用户登录 |
| POST | /api/v1/users/register | 用户注册 |
| GET | /api/v1/model-groups | 获取模型分组 |
| POST | /api/v1/model-groups | 创建模型分组 |
| GET | /api/v1/models | 获取模型列表 |
| POST | /api/v1/models | 创建模型 |
| GET | /api/v1/models/:id/details | 获取模型详情 |
| GET | /api/v1/resources/model/:modelId | 获取资源列表 |
| POST | /api/v1/resources | 创建资源 |
| GET | /api/v1/search | 全局搜索 |
| GET | /api/v1/tags | 获取标签列表 |
| POST | /api/v1/tags | 创建标签 |

### 配置文件说明 (config.yaml)

```yaml
server:
  host: 0.0.0.0
  port: 8080

mongodb:
  host: localhost
  port: 27017
  database: cmdb

ldap:
  enabled: false
  host: ""
  port: 389
  base_dn: ""
  ad:
    enabled: false
    domain: ""
```

### 备注

- 默认管理员账号: admin / admin123 (注册后生效)
- MongoDB需提前启动在27017端口
